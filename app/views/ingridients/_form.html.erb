<%= form_with(model: ingridient, local: true) do |form| %>
  <% if ingridient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingridient.errors.count, "error") %> prohibited this ingridient from being saved:</h2>

      <ul>
        <% ingridient.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, 'Название' %>
    <%= form.text_field :name, class:"enter-name" %>
  </div>

  <div class="field">
    <%= form.label :description, 'Описание' %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit "Добавить", class: "A_Button" %>
  </div>
<% end %>
