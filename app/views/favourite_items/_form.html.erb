<%= form_with(model: favourite_item, local: true) do |form| %>
  <% if favourite_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favourite_item.errors.count, "error") %> prohibited this favourite_item from being saved:</h2>

      <ul>
        <% favourite_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :favourite_id %>
    <%= form.number_field :favourite_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :product_item_id %>
    <%= form.number_field :product_item_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
